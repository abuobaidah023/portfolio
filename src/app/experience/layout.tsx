import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Experience & Case Studies — M Abuobaida Nasir",
  description:
    "Professional timeline and case studies showcasing Local SEO, GBP, and organic growth results.",
};

export default function ExperienceLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
